apiVersion: v1
kind: Service
metadata:
  name: <%= dasherize(applicationName) %>
  namespace: <%= namespace %>
  labels:
    app: <%= dasherize(applicationName) %>
spec:
  selector:
    app: <%= dasherize(applicationName) %>
  ports:
    - port: <%= port %>
      targetPort: <%= port %>
  type: ClusterIP
