{
  "$schema": "http://json-schema.org/schema",
  "$id": "otel-config",
  "title": "OTel Config",
  "description": "Adds OpenTelemetry Collector configuration and configures nginx to proxy OTLP exports",
  "type": "object",
  "properties": {
    "nginxFile": {
      "type": "string",
      "description": "Path to the nginx.conf file to modify",
      "default": "/nginx.conf.template",
      "x-prompt": "Path to the nginx.conf file to modify"
    }
  }
}
