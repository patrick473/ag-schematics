{
  "$schema": "http://json-schema.org/schema",
  "$id": "sonar-config",
  "title": "Sonar Config",
  "description": "Adds SonarQube configuration to the project",
  "type": "object",
  "properties": {
    "projectKey": {
      "type": "string",
      "description": "The SonarQube project key",
      "default": "f-service",
      "x-prompt": "What is the SonarQube project key?"
    },
    "projectName": {
      "type": "string",
      "description": "The SonarQube project name",
      "default": "frontend-service",
      "x-prompt": "What is the SonarQube project name?"
    },
    "organization": {
      "type": "string",
      "description": "The SonarQube organization name",
      "default": "organization",
      "x-prompt": "What is the SonarQube organization name?"
    }
  }
}
